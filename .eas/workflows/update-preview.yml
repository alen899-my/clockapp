version: 1
on:
  push:
    branches:
      - main
jobs:
  publish-preview-update:
    name: Publish preview EAS Update
    type: update
    channel: preview
    platforms:
      - android
      - ios
    auto: true
    message: "Automated OTA update from main branch to preview channel"
    wait: true
